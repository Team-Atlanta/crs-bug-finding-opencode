- [ ] Ran the candidate input against the harness via `libCRS run-pov` with
      original build. Submitting for patch-build is not accepted.
- [ ] `retcode` is non-zero (crash confirmed)
- [ ] Crash is a distinct vulnerability (different from previously found POVs);
      You will be punished for submitting duplicate PoVs.
